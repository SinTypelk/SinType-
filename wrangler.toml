# Cloudflare Pages — connect repo root to /website or set root directory to website in dashboard
name = "sintype-website"
compatibility_date = "2024-01-01"

[site]
# After `npm run build`, deploy the Vite client output:
bucket = "./dist/client"
